<Window x:Class="EasyRank__.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyRank__"
        mc:Ignorable="d"
        Title="EasyRank++" Height="450" Width="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Name="selectedKey"
                 Grid.Row="0"
                 MaxLength="1"
                 Text="S"
                 TextAlignment="Center" />
        <ToggleButton Name="startButton"
                      Grid.Row="1"
                      Checked="StartButton_Checked"
                      Unchecked="StartButton_Unchecked"
                      Content="Start/Stop" />
    </Grid>
</Window>
